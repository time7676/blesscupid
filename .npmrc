# Metro's resolver doesn't follow pnpm's nested .pnpm layout, so the React
# Native + Expo dependency tree has to be hoisted to the workspace root. This
# is the documented Expo + pnpm workaround. Trade-off: fewer phantom-dep
# guarantees from pnpm; gain: `expo export` and Metro bundling actually work.
shamefully-hoist=true
