import en from './en.placeholder.json' with { type: 'json' };

/**
 * Copy keys are STABLE. Values are placeholders pending Pastor sign-off (BLE-7f).
 * Engineering must not author copy here. Replace `en.placeholder.json` with Pastor-signed
 * `en.json` before BLE-7 ships.
 */
export const copy = en;
